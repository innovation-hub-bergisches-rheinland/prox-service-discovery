version: "3.7"

services:

  service-discovery:
    image: docker.nexus.archi-lab.io/archilab/prox-service-discovery
    restart: always
    networks:
      - prox

networks:
  prox:
    external: true
